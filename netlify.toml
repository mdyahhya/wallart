# Netlify Configuration for Main Site
[build]
  functions = "netlify/functions"
  publish = "."

# This plugin automatically installs dependencies inside netlify/functions/package.json
[[plugins]]
  package = "@netlify/plugin-functions-install-core"

[functions]
  node_bundler = "esbuild"
